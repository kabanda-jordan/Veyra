# Never fail on optional packages that don't match current platform
optional=true

# Resolve SWC binaries for all platforms so the lockfile works on Linux too
# This is critical for deploying from Windows to Linux (Render/Railway/Vercel)
